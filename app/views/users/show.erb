<h1><%= @user.username %>'s recent searches:</h1>

<ul>
  <% @searches.each do |search| %>
    <li><%= search.name %><%= search.content.length > 20 ? "  -  #{search.content[0..100]}..." : "  -  No results" %></li>
  <% end %>
</ul>
